<div>
    <table>
        <thead>
                <th>Carrito de: {{cart.user}}</th>
            <tr>
                <th>Producto</th>
                <th>Precio</th>
                <th>Cantidad</th>
            </tr>
        </thead>
        <tbody>
                {{#each cart.products}}
            <tr>
                <td>{{product.nombre}}</td>
                <td>{{product.precio}}</td>
                <td>{{qty}}</td>
            </tr>
                {{/each}}
                 <tr>
                <td>{{totalCarrito}}</td>
            </tr>
        </tbody>
    </table>
</div>